<p id="notice"><%= notice %></p>

<p>
  <strong>Name:</strong>
  <%= @particle.name %>
</p>

<p>
  <strong>Speed:</strong>
  <%= @particle.speed %>
</p>

<%= link_to 'Edit', edit_particle_path(@particle) %> |
<%= link_to 'Back', particles_path %>
